{
  "name": "proappstore-storefront",
  "version": "0.0.0",
  "private": true,
  "description": "Static storefront for ProAppStore. index.html and manifest.json are built from registry.json by build.js (run on Cloudflare Pages).",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/build.test.js"
  }
}
